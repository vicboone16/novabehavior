import { NextUpWidget } from '@/components/dashboard/NextUpWidget';

/** Renders the NextUp widget content inside the dashboard grid shell */
export function NextUpWidgetContent() {
  return <NextUpWidget embedded />;
}
